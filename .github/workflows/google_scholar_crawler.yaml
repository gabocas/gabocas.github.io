name: Update OpenAlex citation count

on:
  workflow_dispatch:
  schedule:
    - cron: "17 4 * * 1"

permissions:
  contents: write

jobs:
  update-citation-count:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r google_scholar_crawler/requirements.txt

      - name: Fetch OpenAlex citation count
        env:
          OPENALEX_AUTHOR_ID: ${{ secrets.OPENALEX_AUTHOR_ID }}
        run: python google_scholar_crawler/main.py

      - name: Commit updated citation data
        run: |
          echo "Repository status:"
          git status --short

          echo "Generated data:"
          ls -la _data
          test -f _data/citations.yml

          git add _data/citations.yml

          if git diff --cached --quiet; then
            echo "Citation data is unchanged; no commit needed."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: update OpenAlex citation count"
          git push origin HEAD:${{ github.ref_name }}
